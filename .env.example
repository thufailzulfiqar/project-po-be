# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=po_db
DB_USER=postgres
DB_PASS=postgress

# JWT
JWT_SECRET=JWT_SECRET_KEY
JWT_EXPIRES_IN=1d

# Upload
UPLOAD_DIR=uploads
MAX_FILE_SIZE_MB=10
